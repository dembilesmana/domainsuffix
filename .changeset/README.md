# Changesets

This repository uses Changesets for versioning and publishing.

Create a changeset when you want to release a new version:

```bash
bunx changeset
```

The GitHub Actions workflow will then open a release PR or publish automatically.
